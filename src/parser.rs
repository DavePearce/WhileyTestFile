pub struct Parser<'a> {
    lines: Vec<&'a str>
}

impl<'a> Parser<'a> {
    /// Construct a new parser from a given string slice.
    pub fn new(input: &'a str) -> Self {
        todo!["split into lines"]
    }
}
